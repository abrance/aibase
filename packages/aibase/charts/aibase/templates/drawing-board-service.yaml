{{- if .Values.drawingBoard.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "aibase.fullname" . }}-drawing-board
  labels:
    {{- include "aibase.labels" . | nindent 4 }}
    app.kubernetes.io/component: drawing-board
  {{- with .Values.drawingBoard.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.drawingBoard.service.type }}
  ports:
    - port: {{ .Values.drawingBoard.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aibase.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: drawing-board
{{- end }}
